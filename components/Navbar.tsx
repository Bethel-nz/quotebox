import { Subscribe } from "./Subscribe";

const Navbar = () => {

  return (
    <div>
      <span className="font-bold text-xl">Quotebox</span>
      <Subscribe />
    </div>
  );
};

export default Navbar;
